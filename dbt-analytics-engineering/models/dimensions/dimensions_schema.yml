models:
  - name: dim_customers
    description: "Customer dimension (one row per customer)."
    columns:
      - name: CUSTOMERID
        tests:
          - not_null
          - unique
      - name: EmailAddress
        tests:
          - not_null
